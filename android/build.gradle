// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        buildToolsVersion = '34.0.0' // Bajamos a 34
        minSdkVersion = 24
        compileSdkVersion = 34 // Bajamos a 34
        targetSdkVersion = 34 // Bajamos a 34
        kotlinVersion = '1.9.25'
        ndkVersion = "26.1.10909125"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath('com.android.tools.build:gradle:8.3.0') // Conservamos versión estable
        classpath('org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.25') // Conservamos versión estable
    }
}

// 🔴 ELIMINAMOS ESTA LÍNEA
// apply plugin: "com.facebook.react.rootproject"

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' }
    }
}
